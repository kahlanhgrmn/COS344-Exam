COS 344: L11 Chapter 23: Visualization
Cobus Redelinghuys
23/04/2026

Visualization
Computer-generated images that are used to help people
understand both spatial and non-spatial data.
- Visualisation can be used by humans to make judgments or
decisions.
- Visualisation allows humans to offload cognition to the
perceptual system, using carefully constructed images as a
form of “external memory”.
- After computer games, visualisation is one of the biggest uses
of computer graphics.
- What do you think is meant by visualisation?

- After computer games, visualisation is one of the biggest uses
of computer graphics.
- What do you think is meant by visualisation?
Visualization
Computer-generated images that are used to help people
understand both spatial and non-spatial data.
- Visualisation can be used by humans to make judgments or
decisions.
- Visualisation allows humans to offload cognition to the
perceptual system, using carefully constructed images as a
form of “external memory”.

- In the 18th century, William Playfair created the first visual
representations of abstract non-spatial datasets.
- Interactive visualisations have become popular recently due to
the increase in available computing power.
- The idea of using pictures to convey meaning is not new.
- What are some old non-digital examples of using pictures to
convey meaning?

- The idea of using pictures to convey meaning is not new.
- What are some old non-digital examples of using pictures to
convey meaning?
- In the 18th century, William Playfair created the first visual
representations of abstract non-spatial datasets.
- Interactive visualisations have become popular recently due to
the increase in available computing power.

- We have spoken about the limited resources in computers, but
we also need to consider the limited resources in users.
- Users have limited memory and attention, and as such, need
to be taken into account when making visualisations.
- Human memory is notoriously limited - The pain of every
student.

- Examples of each?
- Let’s start with a table of data:
- Rows of data are seen as items.
- Columns are dimensions/attributes.
- What is the difference between:
- Quantitative
- Ordered
- Categorical
- Relational data

- Let’s start with a table of data:
- Rows of data are seen as items.
- Columns are dimensions/attributes.
- What is the difference between:
- Quantitative
- Ordered
- Categorical
- Relational data
- Examples of each?

- The hardest thing when visualising data is somehow encoding
higher dimensionality such that it is understandable.
- The number of data items is also important.
- Often, too many data points can confuse the viewer.

- What does derived dimension mean?
- Idea is to reduce the amount of information by calculating a
value that “contains” both data points.
- Example: Say we have a data set that consists of the height,
width and depth of a shape.
- Visualising these three on the same graph might be
problematic, so we would rather calculate the volume of the
shape and just display that on a graph.
- Why is it sometimes useful to change the datatypes of an
attribute of a data point when visualising it?
- Examples?

- Idea is to reduce the amount of information by calculating a
value that “contains” both data points.
- Example: Say we have a data set that consists of the height,
width and depth of a shape.
- Visualising these three on the same graph might be
problematic, so we would rather calculate the volume of the
shape and just display that on a graph.
- Why is it sometimes useful to change the datatypes of an
attribute of a data point when visualising it?
- Examples?
- What does derived dimension mean?

- Why is it sometimes useful to change the datatypes of an
attribute of a data point when visualising it?
- Examples?
- What does derived dimension mean?
- Idea is to reduce the amount of information by calculating a
value that “contains” both data points.
- Example: Say we have a data set that consists of the height,
width and depth of a shape.
- Visualising these three on the same graph might be
problematic, so we would rather calculate the volume of the
shape and just display that on a graph.

- What are some examples of interactions to aid the viewer’s
understanding?
- Hovering over data points to show more information.
- Selecting some data points to be displayed or hiding other data
points.
- How can interactivity help the user to understand the data?

- Hovering over data points to show more information.
- Selecting some data points to be displayed or hiding other data
points.
- How can interactivity help the user to understand the data?
- What are some examples of interactions to aid the viewer’s
understanding?

- How can interactivity help the user to understand the data?
- What are some examples of interactions to aid the viewer’s
understanding?
- Hovering over data points to show more information.
- Selecting some data points to be displayed or hiding other data
points.

- The phrase: “Overview First, Zoom and Filter, Details on
Demand” implies the importance of the role of interaction and
navigation in visual design.
- Overview helps the user identify critical areas.
- After identifying critical areas, spatial navigation or filtering
can help to investigate these areas.

- Interaction also requires more “cost” from the user as they
need to pay attention to what they are doing to fully
understand what is happening.
- Can interaction mislead the viewer?
- Although interactivity can aid the user, it does come with an
increase in computational costs.
- Why?

- Although interactivity can aid the user, it does come with an
increase in computational costs.
- Why?
- Interaction also requires more “cost” from the user as they
need to pay attention to what they are doing to fully
understand what is happening.
- Can interaction mislead the viewer?

- One example: Showing the data change over time.
- Unfortunately, this is the only “really” useful use of
animations in visualisation.
- This is due to the limited resources of humans, specifically our
memory skills.
- Humans will struggle to compare different states of the
animation of data due to not being able to accurately
remember the different states.
- How can animations aid in visualisation?

- How can animations aid in visualisation?
- One example: Showing the data change over time.
- Unfortunately, this is the only “really” useful use of
animations in visualisation.
- This is due to the limited resources of humans, specifically our
memory skills.
- Humans will struggle to compare different states of the
animation of data due to not being able to accurately
remember the different states.

- This is when we display one or two-dimensional data on a flat
single surface.
- Line charts, bar charts, and scatterplots are all examples of
these.

- Multiple items can be overlaid on the same image as long as
they don’t interfere with each other.
- This type of view allows for easier comparisons as everything
is together.

- What about symbols with predefined meanings?
- These are known as glyphs.
- So far, we have only discussed using the actual value or a
transformed version of the actual value.
- But what alternative can we use rather?

- So far, we have only discussed using the actual value or a
transformed version of the actual value.
- But what alternative can we use rather?
- What about symbols with predefined meanings?
- These are known as glyphs.

- Multiple views are normally a set of single views linked
together on the same screen.
- The links tell the user where each data point in one view links
to a data point in another view.

Why don’t computer graphics programmers ever trust what
they see?
